export default interface User {
    _userId: String | null,
    name: String,
    edad: Number,
    numDoc: String,
    typeDoc: String,
    phone: Number,
    email: String,
    city: String,
    token: Number,
    status: Number,
    notView: Number
}